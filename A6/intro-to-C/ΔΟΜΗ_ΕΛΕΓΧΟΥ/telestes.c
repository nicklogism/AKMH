# include <stdio.h>

main()
{
	int integer1;
	printf("Dwse akeraio arithmo:\n");
	scanf("%d",&integer1);
	// H if κλείνει στην printf
	if (integer1%2 == 0)
		printf("Artios arithmos\n");
	// Η else κλείνει στην printf
	else 
		printf("Peritos arithmos");
	// Αν είχαμε δυο εντολές κάτω από την if ή την else θα χρειαζόμασταν άγκιστρα {}
	
}

/* 
Σχεσιακοί Τελεστές (εκτελούνται από αριστερά προς τα δεξιά)
>
<
> =
< =
== (ίσο με)
!= (Διάφορο με)

 Αριθμιτικοί τελεστές
 +
 -
 *
 / (div - Διαίρεση)
 % (Mod - Υπόλοιπο)
 */
